---
parent: plan/0.7.3.3.2.2-evaluate-candidate-locations.md
root: plan/root.md
---
# 0.7.3.3.2.2.3 Create Reproducibility Script [PLAN]

## Description
Develop a short Python script (or Jupyter notebook) that takes the outline from 0.7.3.3.2.1 and automatically computes the evaluation matrix, outputting `artifacts/0.7.3.3.2-candidate-evaluation.yaml`.

## Acceptance Criteria
1. Script `scripts/evaluate_candidate_locations.py` exists and is executable.
2. Running the script without arguments reproduces the matrix file exactly as in subtask 0.7.3.3.2.2.1.
3. The script includes clear inline comments and a usage docstring.
4. Dependencies are listed in `requirements.txt` (e.g., `pyyaml`).
5. The script and any supporting notebooks are committed under `scripts/` and referenced in the parent plan.
