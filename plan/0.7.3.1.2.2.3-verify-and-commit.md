---
parent: plan/0.7.3.1.2.2-insert-mapping.md
root: plan/root.md
---
# 0.7.3.1.2.2.3 Verify and Commit [PLAN]
## Description
Run a lightweight markdown syntax check on `docs/codebase-map.md` and confirm that the edited line now contains the exact mapping `src/plan-guardian/ → 0.7.3.1`. If verification passes, delete the backup; otherwise, restore from backup and report failure.

## Acceptance Criteria
1. A markdown parser confirms the file is syntactically valid after the edit.  
2. The edited line contains the exact substring `src/plan-guardian/ → 0.7.3.1`.  
3. No other lines were altered (checksum comparison with pre‑edit backup).  
4. On success, the backup file is removed; on failure, the original file is restored.  
5. A concise verification report is logged indicating pass/fail status.  
