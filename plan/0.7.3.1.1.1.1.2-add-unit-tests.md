---
parent: plan/0.7.3.1.1.1.1-collect-plan-files.md
root: plan/root.md
---
# 0.7.3.1.1.1.1.2 Add Unit Tests [PLAN]

Write pytest tests in `tests/test_collect_plan_files.py` that:
* Invoke the collector script in a temporary copy of the repo.
* Verify that the resulting JSON contains entries for at least the three known plan files (`root`, `0.7.3`, `0.7.3.1.1.1`).
* Assert that each entry’s `id`, `source`, and `owner` fields are correctly populated.
* Ensure the script exits cleanly when no plan files are present (edge‑case).

**Acceptance Criteria**
1. Test suite runs with `pytest -q` and all tests pass.
2. Test validates presence of the three required plan IDs.
3. Test checks that `owner` equals the value from front‑matter or `plan-guardian` default.
4. Test confirms that `temp/plan_files.json` is created and contains valid JSON.
5. Coverage report shows ≥ 80 % of `collect_plan_files.py` exercised.
