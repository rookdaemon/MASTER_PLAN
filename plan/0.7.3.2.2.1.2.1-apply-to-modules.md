---
parent: plan/0.7.3.2.2.1.2-review-and-finalize-template.md
root: plan/root.md
---
# 0.7.3.2.2.1.2.1 Apply Template to Sample Modules [REVIEW]

## Description
Select two representative modules from the codebase, integrate the draft mapping‑entry template, and verify that all required fields are populated correctly.

## Execution Spec
**Goal:** Produce two functional module implementations that incorporate the mapping‑entry template defined in `0.7.3.2.2.1.1`, and generate a short integration report.

### Files to Create / Modify
1. `modules/sample_module_a/README.md` – add a **Mapping Entry** section using the template.  
2. `modules/sample_module_a/mapping_entry.yaml` – YAML file containing the concrete mapping entry.  
3. `modules/sample_module_b/README.md` – add a **Mapping Entry** section using the template.  
4. `modules/sample_module_b/mapping_entry.yaml` – YAML file containing the concrete mapping entry.  
5. `integration_report.md` – short report summarizing the integration outcome.

### Template (as defined in `0.7.3.2.2.1.1`)

