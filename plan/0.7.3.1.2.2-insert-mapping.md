---
parent: plan/0.7.3.1.2-edit-mapping.md
root: plan/root.md
---
# 0.7.3.1.2.2 Insert Mapping [PLAN]
## Description
Edit the previously‑identified line in `docs/codebase-map.md` to append a clear mapping to feature card `0.7.3.1` (e.g., `src/plan-guardian/ → 0.7.3.1`). Preserve original indentation and surrounding syntax.

## Acceptance Criteria
1. The line now contains the mapping arrow and the exact identifier `0.7.3.1`.  
2. Indentation and any surrounding markdown (tables, bullet lists) remain unchanged.  
3. The file passes a basic syntax check (markdown parsable).  
4. Only `src/plan-guardian/` line is altered; no other lines are modified.  
5. The edit is saved atomically to avoid partial writes.  
