---
parent: plan/0.2.1.1-radiation-hardened-computation.md
root: plan/root.md
blocked-by:
  - plan/0.1.3-consciousness-engineering.md
---
# 0.2.1.1.1 Radiation Shielding & Substrate Hardening [DONE]

## Description

Layers 1 and 2 from the radiation-hardened computation architecture, plus the degradation model. This covers:

1. **Shielding Architecture (Layer 1)** — Graded-Z passive shielding (tungsten → aluminum → polyethylene) that reduces incident radiation flux to substrate-tolerable levels within a 50 kg/m² mass budget.
2. **SEU Mitigation & TID Tolerance (Layer 2)** — SiC substrate with TMR redundancy achieving BER < 10⁻¹² under GCR flux. Material selection for TID tolerance ≥ 1 Mrad.
3. **Degradation Model** — Physics-based model projecting CDD and TID degradation over 1000+ year timescales with annealing recovery.

All three layers are already fully implemented with 22 passing tests. This card should advance rapidly through ARCHITECT and IMPLEMENT to REVIEW.

---

## Decision — Primary Substrate Material

### Context
The radiation-hardened computing substrate must maintain computational integrity over 1000+ year lifetimes under cosmic radiation. The semiconductor material choice determines TID tolerance, SEU cross-section, CDD resistance, thermal performance, and process maturity.

### Options Considered
1. **Silicon Carbide (SiC)**: Wide bandgap (3.26 eV), TID >10 Mrad, excellent CDD resistance, mature process at 180 nm.
2. **Gallium Nitride (GaN)**: Wide bandgap (3.4 eV), high electron mobility, ~5 Mrad TID tolerance, less mature digital process.
3. **Silicon-on-Insulator (SOI)**: Mature 65 nm process, good SEU resistance, but only ~1 Mrad TID — marginal for 1000-year targets.
4. **Diamond semiconductor**: Ultimate radiation hardness (>100 Mrad), but immature process (500 nm), limited foundry availability.

### Choice
**SiC** as primary substrate, with Diamond as future upgrade path.

### Rationale
SiC offers the best balance of TID tolerance (10 Mrad — 10× the 1 Mrad requirement), thermal range (77–873 K), CDD resistance (low NIEL coefficient of 2.0e-3 MeV·cm²/g), and process maturity. Diamond dominates on raw radiation hardness but process immaturity makes it unsuitable today.

### Consequences
- Feature size limited to 180 nm (lower transistor density than SOI at 65 nm).
- 4.3× compute overhead from TMR + failover reserves must be absorbed.
- Diamond migration path should be monitored; if process matures, it supersedes SiC on all metrics.

---

## Decision — Redundancy Strategy

### Context
SEU bit flips from cosmic rays must be reduced to BER < 10⁻¹² per bit-hour. Hardware redundancy is the primary mechanism beyond shielding and inherent substrate hardness.

### Options Considered
1. **Triple Modular Redundancy (TMR)**: Three independent compute lanes with majority voter. Well-understood, deterministic. 3× hardware overhead.
2. **N-Modular Redundancy (NMR, N=5+)**: Higher fault tolerance but 5×+ overhead.
3. **Information Redundancy (ECC-only)**: Error-correcting codes without hardware replication. Lower overhead but cannot correct logic errors, only memory errors.

### Choice
**TMR** as baseline redundancy for all compute lanes, augmented with ECC (SECDED minimum) for memory.

### Rationale
TMR reduces effective BER from p to ~3p² (for small p), which combined with shielding attenuation and SiC's low SEU cross-section, achieves BER < 10⁻¹² under GCR flux. NMR provides diminishing returns at much higher cost. ECC alone cannot protect logic paths.

### Consequences
- 3× hardware overhead for compute lanes, plus 30% failover reserves → 4.3× total.
- Voter logic itself must be hardened (single point of failure).
- TMR integrates naturally with Layer 5 process migration (covered in sibling card 0.2.1.1.3).

---

## Decision — Shielding Architecture

### Context
Incident radiation must be reduced to substrate-tolerable levels while remaining within a mass budget suitable for space deployment (≤ 50 kg/m²).

### Options Considered
1. **Pure high-Z shielding**: Tungsten/tantalum only. Maximum GCR attenuation but generates secondary bremsstrahlung radiation.
2. **Graded-Z shielding**: High-Z outer → graded intermediate → hydrogen-rich inner. Absorbs secondaries progressively.
3. **Active electromagnetic shielding**: Superconducting magnetic coils deflect charged particles. No mass penalty but requires power and cryogenics.

### Choice
**Graded-Z passive shielding** (tungsten → aluminum → polyethylene) as baseline.

### Rationale
Graded-Z avoids the secondary radiation amplification problem of pure high-Z designs. The three-layer stack (W 0.14 cm / Al 0.4 cm / PE 0.8 cm) achieves meaningful attenuation across all particle types at 45.4 kg/m², within the 50 kg/m² budget. Active EM shielding adds complexity and power draw without clear advantage for the baseline case.

### Consequences
- Neutron moderation via polyethylene inner layer is critical — without it, secondary neutrons from high-Z layers would increase SEU rates.
- Mass budget constrains maximum shielding thickness; extreme flux environments may require active augmentation.
- Thermal load from absorbed radiation must be managed (positive but finite at GCR reference flux).

---

## Contracts

### ShieldingModule (Layer 1) — `GradedZShield` class

**Preconditions:**
- `energy_MeV > 0` (non-positive energy throws Error)
- Shield must have ≥ 1 layer (empty layer stack throws Error)
- Layer material must exist in `MATERIAL_ATTENUATION` database

**Postconditions:**
- `attenuationFactor(particleType, energy_MeV)` returns value in `(0, 1]` — lower means more shielding
- `massPerArea()` returns value in kg/m²; must be ≤ 50 for default config
- `thermalLoad()` returns positive finite value in watts/m²
- `meetsmassBudget()` returns `true` for default config

**Invariants:**
- Attenuation factor decreases (more penetration) as particle energy increases
- Mass per area is deterministic for a given layer configuration
- Layer order matters: graded-Z requires high-Z outer, hydrogen-rich inner

### SubstrateSpec & SEU_BER (Layer 2) — `calculateSEU_BER`, `yearsToTIDLimit`, `selectSubstrate`

**Preconditions:**
- `fluxPerCm2PerSec ≥ 0`
- `substrate` must have valid `seuCrossSection_cm2 > 0`
- `annualDose_rad > 0` for `yearsToTIDLimit` (returns Infinity for ≤ 0)

**Postconditions:**
- `calculateSEU_BER()` returns errors per bit per hour; must be < 10⁻¹² for SiC under GCR flux with default shielding and TMR
- `yearsToTIDLimit()` returns years until TID tolerance limit
- `selectSubstrate()` returns best candidate or null if none qualifies

**Invariants:**
- TMR reduces BER from p to 3p² (always improves when p < 1/3)
- Shielding attenuation multiplies into flux before BER calculation
- `SUBSTRATE_DATABASE` entries are immutable reference specifications

### DegradationModel — `simulateDegradation`, `meetsCDDCriterion`

**Preconditions:**
- `years > 0`, `stepSize_years > 0`
- `substrate.material` must have a NIEL coefficient (falls back to SOI if missing)
- `operatingTemp_K > 0`

**Postconditions:**
- Returns array of `DegradationModelResult` at each time step
- `performanceFraction` in `[0, 1]` — monotonically non-increasing over time
- `cumulativeTID_rad` and `displacementDamageDose` monotonically increasing
- `meetsCDDCriterion()` returns true iff final `performanceFraction > 0.95`

**Invariants:**
- Higher operating temperature → better annealing → higher performance fraction
- TID degradation follows logarithmic saturation: `0.05 * log10(1 + TID / (tolerance * 0.1))`
- CDD degradation is linear with annealing recovery: `displacementDose * 1e-10 * (1 - 0.3 * annealingNormalized)`
- Combined degradation uses multiplicative independence: `1 - (1-TID_deg)(1-CDD_deg)`

---

## Threshold Registry

| Name | Value | Unit | Valid Range | Rationale | Sensitivity |
|------|-------|------|-------------|-----------|-------------|
| SEU BER target | 10⁻¹² | errors/bit-hour | (0, 10⁻¹⁰] | Standard rad-hard BER threshold for mission-critical computation | 10× margin above target still acceptable |
| TID tolerance minimum | 1×10⁶ | rad | [10⁵, 10⁹] | 1000 years at 100 rad/year interplanetary dose; 10× margin with SiC | Linear with mission lifetime |
| CDD degradation limit | 5% | performance loss | (0%, 50%] | Max acceptable capacity reduction over operational lifetime | SiC achieves ~1.8% at 1000yr — 2.8× margin |
| Shielding mass budget | 50 | kg/m² | [10, 200] | Practical limit for space deployment; default stack is 45.4 kg/m² | Directly constrains maximum layer thicknesses |
| GCR reference flux | 4 | particles/cm²/s | [2, 10] | Solar-minimum interplanetary GCR flux (worst case for SEU) | BER scales linearly with flux |
| Reference energy | 100 | MeV | [10, 10⁴] | Baseline for attenuation coefficient lookup | Energy scaling uses (E_ref/E)^0.3 power law |
| W layer thickness | 0.14 | cm | [0.05, 1.0] | Balances GCR attenuation vs. mass budget and secondary generation | Dominant mass contributor (19.3 g/cm³) |
| Al layer thickness | 0.4 | cm | [0.1, 2.0] | Intermediate graded-Z absorber for bremsstrahlung | Moderate mass contributor |
| PE layer thickness | 0.8 | cm | [0.2, 5.0] | Hydrogen-rich neutron moderator | Critical for neutron attenuation |
| CDD sensitivity factor | 10⁻¹⁰ | dimensionless | [10⁻¹², 10⁻⁸] | Scales displacement damage dose to performance degradation | Directly determines CDD degradation rate |
| Default shielding attenuation (GCR) | 0.69 | ratio | (0, 1) | Graded-Z shield transmission for protons at 1 GeV | Feeds into SEU BER calculation |
| Annealing activation energy | 0.4 | eV | [0.2, 1.0] | Typical Si-based defect annealing energy | Determines temperature sensitivity of self-healing |
| TMR hardware overhead | 4.3× | multiplier | [3.0, 6.0] | 3× for TMR + 30% failover reserves | Defines minimum hardware for fault tolerance |

---

## Behavioral Spec

### Scenario 1: SEU BER Calculation (inline Given/When/Then)

**Given** a SiC substrate with SEU cross-section 10⁻¹⁴ cm², GCR flux of 4 particles/cm²/s, default shielding attenuation of 0.69, and TMR enabled
**When** `calculateSEU_BER` is called
**Then** the effective flux is `4 × 0.69 = 2.76 p/cm²/s`, raw BER is `10⁻¹⁴ × 2.76 × 3600 ≈ 9.94×10⁻¹¹ /bit-hr`, TMR reduces to `3 × (9.94×10⁻¹¹)² ≈ 2.96×10⁻²⁰ /bit-hr`, which is < 10⁻¹² ✓

### Scenario 2: Degradation Model Monotonicity

**Given** a SiC substrate at 300 K operating temperature, 100 rad/year dose, 4 p/cm²/s flux
**When** `simulateDegradation` is run for 1000 years with 10-year steps
**Then** each successive `performanceFraction` is ≤ the previous; `cumulativeTID_rad` and `displacementDamageDose` are strictly increasing; final `performanceFraction > 0.95`

### Scenario 3: Temperature-Dependent Annealing

**Given** identical SiC degradation parameters except `operatingTemp_K`
**When** `simulateDegradation` is run at 200 K and 400 K for 1000 years
**Then** the 400 K result has higher `performanceFraction` than the 200 K result at every time step (annealing recovery is better at higher temperature)

---

## File Manifest
- plan/root.md
- plan/0.2.1-enduring-substrates.md
- plan/0.2.1.1-radiation-hardened-computation.md
- docs/radiation-hardened-computation/ARCHITECTURE.md
- src/radiation-hardened-computation/types.ts
- src/radiation-hardened-computation/shielding.ts
- src/radiation-hardened-computation/substrate.ts
- src/radiation-hardened-computation/degradation-model.ts
- src/radiation-hardened-computation/index.ts
- src/radiation-hardened-computation/__tests__/acceptance.test.ts
- package.json
- tsconfig.json

## Acceptance Criteria
- **AC1 — SEU tolerance**: `calculateSEU_BER` returns BER < 10⁻¹² per bit-hour for SiC substrate under GCR flux (4 p/cm²/s) with default shielding attenuation (0.69) and TMR enabled
- **AC2 — TID tolerance**: SiC substrate `yearsToTIDLimit` returns ≥ 1000 years at 100 rad/year interplanetary dose rate
- **AC3 — CDD tolerance**: `meetsCDDCriterion` returns true for SiC at 1000 years (performance degradation < 5%)
- **AC4 — Shielding mass budget**: Default graded-Z shield configuration `massPerArea` ≤ 50 kg/m²
- **AC5 — CME attenuation**: Shield provides > 50% proton flux reduction at 50 MeV (attenuation factor < 0.50)
- **AC6 — Diamond viability**: Diamond substrate degradation model shows performanceFraction > 0.99 at 100 years
- **AC7 — Degradation model physics**: Performance fraction is monotonically non-increasing; higher temperature yields better annealing; CDD + TID combine multiplicatively

## Revision History
- 2026-03-21T12:00:18.564Z: status REVIEW → DONE
- 2026-03-21: status REVIEW → DONE — all 8 REVIEW exit criteria verified: (1) 7 acceptance criteria met, (2) all Contracts preconditions have guards, postconditions tested, invariants hold, (3) all 3 Decisions (SiC substrate, TMR redundancy, graded-Z shielding) reflected in code, (4) all 13 Threshold Registry values match code constants, (5) all 3 Behavioral Spec scenarios have passing tests, (6) File Manifest accurate, (7) no sibling regressions (7 todo tests belong to 0.2.1.1.2/0.2.1.1.3), (8) 27/27 tests pass clean.
- 2026-03-21T11:59:12.574Z: status IMPLEMENT → REVIEW
- 2026-03-21: status IMPLEMENT → REVIEW — added 5 missing precondition guards: (1) calculateSEU_BER: fluxPerCm2PerSec < 0, (2) calculateSEU_BER: seuCrossSection_cm2 ≤ 0, (3) simulateDegradation: years ≤ 0, (4) simulateDegradation: stepSize_years ≤ 0, (5) simulateDegradation: operatingTemp_K ≤ 0. Added 5 corresponding test cases. All 27 tests pass.
- 2026-03-21T11:55:45.256Z: status REVIEW → IMPLEMENT
- 2026-03-21: status REVIEW → IMPLEMENT — regressed: 5 Contracts precondition guards missing from implementation. (1) calculateSEU_BER: no guard for fluxPerCm2PerSec < 0, (2) calculateSEU_BER: no guard for seuCrossSection_cm2 ≤ 0, (3) simulateDegradation: no guard for years ≤ 0, (4) simulateDegradation: no guard for stepSize_years ≤ 0, (5) simulateDegradation: no guard for operatingTemp_K ≤ 0. All other REVIEW exit criteria pass (ACs verified, Decisions match, Threshold Registry matches, Behavioral Specs tested, tests pass).
- 2026-03-21T11:53:08.750Z: status IMPLEMENT → REVIEW
- 2026-03-21: status IMPLEMENT → REVIEW — all 9 IMPLEMENT exit criteria verified: 22/22 tests pass, all Contracts guards/assertions present, all Decisions reflected in code, all 13 Threshold Registry constants appear with specified values, all 3 Behavioral Spec scenarios have passing tests. 7 todo tests belong to sibling cards 0.2.1.1.2/0.2.1.1.3.
- 2026-03-21T11:52:06.026Z: status ARCHITECT → IMPLEMENT
- 2026-03-21T11:49:47.899Z: status PLAN → ARCHITECT
- 2026-03-21: status ARCHITECT → IMPLEMENT — authored Decision (×3: substrate, redundancy, shielding), Contracts (×3: ShieldingModule, SubstrateSpec/SEU_BER, DegradationModel), Threshold Registry (13 entries scoped to Layers 1/2 + degradation model), Behavioral Spec (×3 scenarios scoped to this card)
