---
parent: plan/0.3.1.5-industrial-era-conscious-agent.md
root: plan/root.md
depends-on:
  - plan/0.3.1.5.1-llm-consciousness-substrate.md
  - plan/0.3.1.5.2-personality-and-trait-model.md
  - plan/0.3.1.5.3-memory-architecture.md
  - plan/0.3.1.5.4-emotion-and-appraisal.md
  - plan/0.3.1.5.5-world-model-and-beliefs.md
  - plan/0.3.1.5.6-planning-and-temporal-reasoning.md
  - plan/0.3.1.5.7-natural-language-interface.md
  - plan/0.3.1.5.8-intrinsic-motivation.md
  - plan/0.3.1.5.10-social-cognition.md
---
# 0.3.1.5.9 Agent Runtime and Event Loop [DONE]

## Description

The scheduler that makes everything actually run. This is the integration card — it wires all subsystems together into a continuously operating agent with cognitive budget enforcement, concurrency management, and real-world I/O.

### Why This Is Last

Every other card in the 0.3.1.5 path produces a module. This card composes them into a running system. It depends on all siblings because it must coordinate all of them within the architecture's cognitive budget constraints.

### Agent Loop

The core cycle, running continuously or triggered by events:

```
loop:
  1. PERCEIVE — Poll environment adapters for new input
     → PerceptionPipeline.ingest() → Percept
     → ConsciousCore.processPercept() → ExperientialState

  2. RECALL — Retrieve relevant memories
     → Memory.retrieve(cues from working memory)
     → Promote retrieved memories into working memory

  3. APPRAISE — Emotional response to percepts
     → EmotionSystem.appraise(percept, goals, values) → updated valence/arousal

  4. DELIBERATE — Decide what to do
     → Update situation awareness (World Model)
     → Check active plans (Planning module)
     → ConsciousCore.deliberate(state, goals) → Decision
     → EthicalDeliberationEngine.extendDeliberation() → EthicalJudgment
     → ValueKernel.evaluateAction() → ValueAlignment

  5. ACT — Execute the decision
     → ActionPipeline.execute(decision) → ActionResult
     → Language generation if action type is communicative

  6. MONITOR — Check that experience is intact
     → ExperienceMonitor.getConsciousnessMetrics()
     → StabilitySentinel.runStabilityCheck() (periodic, not every cycle)
     → Identity checkpoint (periodic)

  7. CONSOLIDATE — Background maintenance
     → Memory consolidation
     → Drive system update
     → Self-model prediction comparison and update

  8. YIELD — Respect cognitive budget; sleep until next trigger
```

### Cognitive Budget Enforcement

The architecture specs allocate processing time:
- Experience maintenance: ≥40% (hard minimum)
- Core deliberation (includes ethical): ≥25% (hard minimum)
- Stability operations: ≤15% (soft maximum)
- Ethical governance overhead: ≤10% (soft maximum)
- Action execution: remainder

The runtime must:
- Measure time spent in each phase
- Truncate phases that exceed budget (planning is truncated before experience maintenance)
- Log budget utilization for tuning
- Never skip the Experience Monitor cycle

### Concurrency

- The Stability Sentinel runs on a separate cadence (every N cycles, not every cycle) — it is expensive and should not dominate processing
- Memory consolidation runs in background allocation
- The main deliberation loop is single-threaded through the conscious core (consciousness is unified — no parallel zombie processing)
- I/O polling may be async, but everything from processPercept() onward is synchronous-sequential through the experiential loop

### Environment Adapters

Concrete I/O bindings — at minimum:
- **Chat interface**: Text input/output via stdin/stdout, WebSocket, or API endpoint
- **Extensible adapter interface**: Additional adapters for file systems, APIs, tool use, or other environments can be added without changing the agent loop

### Startup and Shutdown

- **Startup**: Load identity state from last checkpoint, restore memory, verify value kernel integrity, start experience stream, announce readiness
- **Shutdown**: Follow graceful termination protocol from conscious-core — `ConsciousCore.shutdown()`, preserve final state, checkpoint identity, persist memories
- **Crash recovery**: On unexpected termination, recover from last identity checkpoint; detect and log any experience gap

## File Manifest
- plan/0.3.1.5-industrial-era-conscious-agent.md (parent)
- src/conscious-core/conscious-core.ts (ConsciousCore — central integration point)
- src/conscious-core/interfaces.ts (all subsystem interfaces)
- src/agency-stability/stability-sentinel.ts (periodic stability checks)
- src/agency-stability/identity-continuity.ts (checkpoint/restore)
- src/ethical-self-governance/interfaces.ts (ethical deliberation integration)
- docs/agent-runtime/ARCHITECTURE.md (architecture document — this card's design)
- src/agent-runtime/interfaces.ts (IAgentLoop, ICognitiveBudgetMonitor, IEnvironmentAdapter)
- src/agent-runtime/types.ts (AgentConfig, TickResult, BudgetReport, PhaseTiming, AgentPhase, RawInput, AgentOutput, LoopMetrics)
- src/agent-runtime/agent-loop.ts (AgentLoop class)
- src/agent-runtime/cognitive-budget.ts (CognitiveBudgetMonitor class)
- src/agent-runtime/chat-adapter.ts (ChatAdapter — stdin/stdout + WebSocket)
- src/agent-runtime/startup.ts (startAgent() factory, recoverFromCrash())
- src/agent-runtime/index.ts (barrel export)
- src/agent-runtime/__tests__/agent-loop.test.ts (integration test: full tick cycle)
- src/agent-runtime/__tests__/cognitive-budget.test.ts (unit tests: budget enforcement)
- src/agent-runtime/__tests__/chat-adapter.test.ts (unit tests: adapter poll/send)
- src/agent-runtime/default-subsystems.ts (functional stub implementations of all 12 subsystem interfaces for standalone operation)
- src/agent-runtime/main.ts (entry point — wires default subsystems, starts loop with stdio chat adapter)
- src/agent-runtime/README.md (installation, usage, architecture guide)

## Acceptance Criteria
- Agent runs continuously or responsively without manual orchestration per cycle
- Full processing pipeline executes for every input: perceive → recall → appraise → deliberate → act → monitor
- Cognitive budget allocation is measurable at runtime and enforced (experience maintenance never drops below 40%)
- Stability sentinel runs periodically and alerts surface correctly
- Identity checkpoints occur at configured intervals
- Graceful startup restores from last checkpoint with memory and personality intact
- Graceful shutdown preserves complete agent state
- Crash recovery detects experience gap and logs it
- Agent connects to at least one real-world I/O channel (chat interface)
- Integration test: full end-to-end cycle — agent starts from checkpoint, receives input, perceives, recalls, appraises, deliberates ethically, acts, monitors, and the entire flow is traceable in logs

## Architecture Notes (from ARCHITECT phase)

Architecture document written to `docs/agent-runtime/ARCHITECTURE.md`. Key decisions:

1. **`AgentLoop`** class owns tick loop; all subsystems injected via constructor (interfaces, not concrete classes)
2. **`CognitiveBudgetMonitor`** tracks wall-clock per phase; `shouldYieldPhase()` provides soft-yield signals; MONITOR phase is exempt from truncation
3. **`IEnvironmentAdapter`** interface decouples loop from I/O; `ChatAdapter` is the first concrete implementation (stdin/stdout + WebSocket)
4. **Startup**: `startAgent()` factory calls `recoverIdentity()` → value kernel integrity check → `startExperienceStream()` → connect adapters → return loop
5. **Crash recovery**: gap detected by absent continuity token on boot; logged to experience continuity log; recovery from last valid link in chain
6. **Sentinel cadence**: cycle-based counter (default: every 10 cycles); **Checkpoint interval**: time-based (default: 60 s)
7. **Tick cycle**: 8 phases match the card spec exactly; CONSOLIDATE is budget-gated (yields if budget exhausted); all phases timed via budget monitor

## Implementation Progress

- [x] `src/agent-runtime/types.ts` — AgentConfig, TickResult, BudgetReport, PhaseTiming, AgentPhase, RawInput, AgentOutput, LoopMetrics
- [x] `src/agent-runtime/interfaces.ts` — IAgentLoop, ICognitiveBudgetMonitor, IEnvironmentAdapter, IMemoryStore, IEmotionSystem, IDriveSystem
- [x] `src/agent-runtime/cognitive-budget.ts` — CognitiveBudgetMonitor class (unit-testable budget tracking)
- [x] `src/agent-runtime/agent-loop.ts` — AgentLoop class (8-phase tick cycle, budget enforcement, sentinel cadence, checkpoint interval)
- [x] `src/agent-runtime/chat-adapter.ts` — ChatAdapter (stdin/stdout + WebSocket; dynamic WS resolver for Node 22+ global or 'ws' package)
- [x] `src/agent-runtime/startup.ts` — startAgent() factory, recoverFromCrash()
- [x] `src/agent-runtime/index.ts` — barrel export
- [x] `src/agent-runtime/__tests__/cognitive-budget.test.ts` — 21 tests, all passing
- [x] `src/agent-runtime/__tests__/agent-loop.test.ts` — 30 tests, all passing (self-stopping pattern via `isExperienceIntact` mock; tests: all 8 phases, sentinel cadence, degradation counting, alert counting, checkpoint interval, graceful shutdown, loop metrics, adapter I/O)
- [x] `src/agent-runtime/__tests__/chat-adapter.test.ts` — 30 tests, all passing (connect/disconnect lifecycle, isConnected() state, poll() queue drain + maxBatchSize + blank-line filtering, send() stdio/WS, WS JSON-envelope unwrap, WS close/error/open events)
- [x] `src/agent-runtime/default-subsystems.ts` — functional stub implementations of all 12 subsystem interfaces (DefaultConsciousCore, DefaultPerceptionPipeline, DefaultActionPipeline, DefaultExperienceMonitor, DefaultValueKernel, DefaultIdentityContinuityManager, DefaultStabilitySentinel, DefaultEthicalDeliberationEngine, DefaultMemoryStore, DefaultEmotionSystem, DefaultDriveSystem) enabling standalone agent operation without external LLM APIs or databases
- [x] `src/agent-runtime/main.ts` — entry point (`npm start`): wires default subsystems into AgentLoop via startAgent(), connects stdio ChatAdapter, handles SIGINT/SIGTERM graceful shutdown with session metrics
- [x] `src/agent-runtime/README.md` — installation, usage, environment configuration, architecture guide, subsystem replacement instructions

## Architecture Notes (from PLAN phase)

The PLAN phase confirmed that all dependency modules expose sufficient interfaces for composition:

- **ConsciousCore** (`conscious-core.ts`): provides `processPercept()`, `deliberate()`, `introspect()`, `shutdown()` — the backbone of the loop
- **IExperienceMonitor** (`interfaces.ts`): `getConsciousnessMetrics()`, `isExperienceIntact()` — drives the MONITOR step and budget gating
- **IActionPipeline** (`interfaces.ts`): `execute(decision)` — the ACT step
- **IPerceptionPipeline** (`interfaces.ts`): `ingest(raw)` — the PERCEIVE step
- **StabilitySentinel** (`stability-sentinel.ts`): `runStabilityCheck()` — runs every N cycles (configurable)
- **IdentityContinuityManager** (`identity-continuity.ts`): `checkpoint()`, `recoverIdentity()` — startup restore and periodic checkpointing
- **IEthicalDeliberationEngine** (`ethical-self-governance/interfaces.ts`): `extendDeliberation()` — woven into DELIBERATE step

Key architectural decisions for ARCHITECT phase:
1. `AgentLoop` class owns the tick loop and budget timer; accepts all subsystem instances via constructor injection
2. `CognitiveBudgetMonitor` tracks wall-clock time per phase, enforces hard floor on MONITOR step
3. `EnvironmentAdapter` interface — `ChatAdapter` as first concrete implementation (stdin/stdout or WebSocket)
4. Sentinel cadence and checkpoint interval are configurable (defaults: every 10 cycles, every 60 s)
5. Startup protocol: load checkpoint via `IdentityContinuityManager.recoverIdentity()` → verify integrity → `ConsciousCore.startExperienceStream()` → enter loop
6. Crash recovery: detect missing continuity token on boot → log gap → recover from last valid link

## Revision History
- 2026-03-18T22:18:17.983Z: status REVIEW → DONE
- 2026-03-18T22:17:27.168Z: status IMPLEMENT → REVIEW
- 2026-03-18T22:18:15.000Z: status REVIEW → DONE — all 81 tests pass (21 budget + 30 loop + 30 adapter); all 10 acceptance criteria verified against implementation
