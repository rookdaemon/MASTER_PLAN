---
parent: plan/0.7.3.1.2.1-design-crossref-schema.md
root: plan/root.md
---
# 0.7.3.1.2.1.2 Define Entities & Relationships [PLAN]

Populate the schema with concrete definitions for the four entities (`requirement`, `design-decision`, `code`, `test`) and the three relationship types (`references`, `implemented-by`, `validated-by`), including required metadata fields.

**Acceptance Criteria**
1. Each entity block lists required fields: `id` (unique string), `title` (short description), `source-path` (file location), and optional `description`.  
2. Relationship definitions specify source and target entity types and cardinality (e.g., many‑to‑many).  
3. All fields are documented with data types and example values inside the YAML file.  
4. A small validation script (`scripts/validate-schema.py`) can load the schema and confirm that all required fields are present for each entity type.  
5. Unit tests (`tests/test_schema.py`) assert that the schema contains exactly the four entities and three relationships.
