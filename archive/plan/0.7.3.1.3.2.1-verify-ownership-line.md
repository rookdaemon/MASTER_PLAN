---
parent: plan/0.7.3.1.3.2-generate-report.md
root: plan/root.md
blocked-by: []
depends-on: []
---
# 0.7.3.1.3.2.1 Verify ownership entry [PLAN]

## Description
Open `docs/codebase-map.md` and check that the exact line `src/plan-guardian/ → 0.7.3.1` appears exactly once.

## Acceptance Criteria
1. The file `docs/codebase-map.md` is readable.  
2. A grep (or equivalent) finds exactly one occurrence of the string `src/plan-guardian/ → 0.7.3.1`.  
3. If the line is missing or appears multiple times, the subtask fails with a clear error message.  
4. The result (found / not found) is written to `artifacts/ownership-check.json` as `{ "found": true }` or `{ "found": false }`.  
5. The subtask prints the path to the JSON artifact on stdout.  
