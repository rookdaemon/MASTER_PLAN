---
parent: plan/0.7.3.3.2.1-extract-section-outline.md
root: plan/root.md
---
# 0.7.3.3.2.1.2 Generate Outline YAML [PLAN]

## Description
Execute the extraction script to produce `artifacts/0.7.3.3.2-section-outline.yaml` containing the full outline of H2 headings from the resilience plan.

## Acceptance Criteria
1. The resulting YAML file exists at `artifacts/0.7.3.3.2-section-outline.yaml`.
2. Each entry includes `heading: "## <text>"`, `text: "<exact heading text>"`, and `line: <integer line number>`.
3. The file validates against a simple schema (list of mappings with those three keys).
4. No modifications are made to any source markdown files.
5. The script execution is logged with a brief console output confirming success.
