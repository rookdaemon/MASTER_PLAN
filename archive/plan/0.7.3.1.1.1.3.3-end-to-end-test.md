---
parent: plan/0.7.3.1.1.1.3-generate-provenance-output.md
root: plan/root.md
---
# 0.7.3.1.1.1.3.3 End‑to‑End Provenance Full‑Cycle Test [PLAN]

Write a comprehensive test that runs the three preceding scripts in order and validates the final state.

**Acceptance Criteria**
1. The test `tests/test_provenance_full_cycle.py` executes:
   - `scripts/collect_plan_files.py`,
   - `scripts/collect_artifact_hashes.py`,
   - `scripts/build_provenance.py`.
2. After execution, `artifacts/provenance/provenance.json` exists and contains at least 5 entries (combined plan files and artifacts).
3. The test confirms that a Git commit with message `"chore: add provenance inventory"` exists and includes both the JSON and markdown files.
4. The test fails if any step exits with a non‑zero status or if the JSON schema validation fails.
5. The test is marked with `@pytest.mark.integration` and runs successfully in CI.
